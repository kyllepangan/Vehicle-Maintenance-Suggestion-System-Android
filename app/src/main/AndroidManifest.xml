<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lbycpd2_test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LBYCPD2_TEST">
        <activity android:name=".ForgotPasswordActivity" android:parentActivityName=".LoginActivity"></activity>
        <activity
            android:name=".MessageActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".InstructionActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".AirFilterActivity"
            android:parentActivityName=".InstructionActivity" />
        <activity
            android:name=".CarWashActivity"
            android:parentActivityName=".InstructionActivity" />
        <activity
            android:name=".ProvideActivity"
            android:parentActivityName=".ChooseVehicleActivity" />
        <activity
            android:name=".AboutUsActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".ChooseVehicleActivity"
            android:parentActivityName=".HomeActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".LoginActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>